namespace Microsoft.Win32
{
    using System;

    public enum IMAGE : uint
    {
        IMAGE_BITMAP = 0,
        IMAGE_CURSOR = 2,
        IMAGE_ENHMETAFILE = 3,
        IMAGE_ICON = 1
    }
}

