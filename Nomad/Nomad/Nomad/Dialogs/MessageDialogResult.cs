namespace Nomad.Dialogs
{
    using System;

    public enum MessageDialogResult
    {
        None,
        OK,
        Cancel,
        Yes,
        No,
        YesToAll,
        NoToAll,
        Skip,
        SkipAll,
        Abort,
        Retry,
        Ignore,
        Shield
    }
}

